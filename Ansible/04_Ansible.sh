file $(which ansible) 

file $(which ansible-playbook) 



The command executed by Ansible is a script written in Python. The command to execute a
Playbook, (Ansible Scripts), is a link back to the Ansible Python script and executes the supplied
Playbook file. You do not need to know Python to use Ansible, but it will help as your
knowledge of Ansible progresses.